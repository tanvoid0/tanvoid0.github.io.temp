const server = `https://tanvoid0-api.herokuapp.com`;
// const server = `http://localhost:5000`;
const portfolioUrl = `${server}/api/portfolio`;
const cdn = `${server}/cdn/projects`;
export {
  server,
  portfolioUrl,
  cdn
}